Acá mostramos la lista de indicadores <br>


<% @indicatorlist.each do |ind| %>


  <% text = ind.id.to_s + "-" + ind.name_indicator %>

  <br><br>

  <%= link_to text, {:controller => "indicators",
                       :action => "index",
                       :ind_id => ind.id }%>

<% end %>
